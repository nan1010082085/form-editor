# Editor UI/UX Fix Implementation Plan

> **状态: 🗄 ARCHIVED / CLOSED（2026-08-12）**  
> 勿再据此开工。活跃计划 → [../2026-08-12-ui-ux-path-to-a.md](../2026-08-12-ui-ux-path-to-a.md)  
> 本文仅作 T0–T9 与 R1–R3 历史归档（含走查证据，保留不删）。

**Goal:** 按 UI/UX 审计 P0→P2 收敛 editor 壳层的无障碍、视觉令牌一致性、**缺失 i18n** 与关键可用性缺口，不改业务逻辑。

**Architecture:** 只动 `@editor`（禁止改 server / shared / docs）。以「企业蓝扁平」为唯一视觉真源；无障碍补丁优先落在活跃壳层（`EditorViewToolbar` / `EditorView` / `PropertyPanel` / `AppLayout`），再清死代码与残留皮肤。

**Tech Stack:** Vue 3 + SCSS Modules + Element Plus + AppIcon + Vitest

**Source audit (R1 静态, 已归档看板):** [editor-ui-ux-audit.canvas.tsx](/Users/yangdongnan/.cursor/projects/Users-yangdongnan-work-schema-platform-server/canvases/editor-ui-ux-audit.canvas.tsx)

**收口看板 (已关闭):** [editor-ui-ux-fix-plan.canvas.tsx](/Users/yangdongnan/.cursor/projects/Users-yangdongnan-work-schema-platform-server/canvases/editor-ui-ux-fix-plan.canvas.tsx)

**实现提交:**
- `da73e1a` — `fix(ui): editor UI/UX audit fixes T0-T9`
- `464e466` — R3 残余（面板 inert、isNarrow mql、widget i18n、缩放 aria）
- `b3b245f` — Round2 文档收尾

**状态:** ✅ **本轮已收口**（2026-08-11）→ 🗄 **已迁入 archive/**（2026-08-12）  
**R3 / Round2 复核:** 通过；未完成延期项已迁入 Path to A（A1–A12）

## Global Constraints

- 禁止跨项目修改（仅 `editor/`）
- 禁止 git 回滚；渐进提交
- 图标只用已注册 AppIcon name
- 编辑器 UI 与画布 Widget 样式隔离（`editor-ui-tokens` 边界不可破坏）
- 不引入新主题切换；本次以清理 neon/glass 残留为主
- 每个 Task 结束后：相关组件可手动验证 + 必要时 `pnpm test` / 类型检查

## File Map

| 区域 | 主要文件 |
|------|----------|
| i18n | `src/locales/editor-zh-CN.ts`, `editor-en-US.ts`；引用方 `AppLayout.vue`, `NotFoundView.vue`, `WidgetMarketView.vue`, `EditorViewToolbar.vue`, canvas/zoom 文案 |
| 活跃 Toolbar | `src/views/EditorViewToolbar.vue`, `src/views/EditorView.module.scss` |
| 死 Toolbar | `src/components/Editor/EditorToolbar.vue`, `EditorToolbar.module.scss` |
| 门户 | `src/views/InstancesView.vue`, `AppLayout.vue`, Login（platform-shared） |
| 左右/属性面板 | `EditorLeftPanel.module.scss`, `style.module.scss`, `PropertyField.module.scss`, `EventLogPanel.module.scss`, `ZoomIndicator.module.scss` |
| 焦点 | `PageTabBar.module.scss`, `WidgetMarketView.vue`, richtext（谨慎，画布内） |
| 主题死代码 | `src/styles/tech-theme.scss` |
| 确认框 | `src/widgets/advanced-table/useAdvancedTableEvents.ts` |
| 令牌扩展 | `src/styles/variables.scss`, 可选新建 `src/styles/a11y.scss` / `z-index` tokens |

---

## Round 2 浏览器走查（2026-08-11 · localhost:5100）

**方法:** Chrome DevTools MCP · 登录 `admin` · 页面：Login → Instances → Editor(`Flex 交互实测`) → Widget Market  
**视口:** 1143×943（已低于计划中的 1280 桌面阈值）

### R1 → R2 对比结论

| R1 静态结论 | R2 实机验证 | 计划调整 |
|-------------|-------------|---------|
| Toolbar 普遍缺 aria-label | **部分已有**：返回/面板/缩放/预览/发布等有 label；仍有 **6 个** 28×28 无标签按钮 | T1 改为「补齐残留 6 个 + 实例卡片操作」 |
| 属性标签 70px 截断 | **确认**：`.label` 实测 width=70；可见「列最小宽…」「列最大宽…」「内容最大…」 | T4 优先级保持，证据升级为实机 |
| icon 热区 28×28 | **确认**：首屏 toolbar 按钮均为 28×28 | T6 保持 |
| neon/glow 污染活跃壳 | 活跃 Toolbar 发布按钮 `box-shadow: none`；企业蓝 `#0060a2` | T3 仍清死代码/glass fallback，但对活跃壳优先级略降 |
| 窄屏未策略 | **1143px 仍双栏全开**，画布被挤压 | T8 提前到 Week2 可考虑 |
| （R1 未覆盖）i18n | **大面积 key 泄漏**（侧栏/404/市场/语言按钮/画布 hint） | **新增 T0，最高优先** |
| （R1 未覆盖）登录无障碍 | placeholder-only「用户名/密码」；无可见 label | 记入跨项目 / 或 editor 包装层备注 |
| （R1 未覆盖）未登录跳转 | `/instances` → `/schema-platform/login` → **本机 404** | 跨项目（platform-shared `authPaths`）；本仓仅能补 404 文案 |

### R2 新发现问题清单

| ID | 严重度 | 现象 | 证据 | 归属 Task |
|----|--------|------|------|-----------|
| R2-01 | P0 | 侧栏/品牌/语言显示 raw key | `editor.appLayout.*`、`editor.toolbar.langEn` | **T0** |
| R2-02 | P0 | 404 页 raw key | `editor.errorPage.notFoundMessage` / `backHome` | **T0** |
| R2-03 | P0 | Widget Market 页头/筛选 raw key | `editor.widgetMarket.title` 等 | **T0** |
| R2-04 | P0 | 画布追加区 raw key | `editor.canvas.flowAppendHint`；a11y 还有 `dragReorder` | **T0** |
| R2-05 | P0 | Zoom 控件 description 为 raw key | `editor.zoomIndicator.zoomOut/In` | **T0** |
| R2-06 | P0 | 独立 dev 未登录跳 `/schema-platform/login` 404 | `authPaths.resolveLoginUrl` → shell base | 跨项目；本仓 T0 修 404 文案 |
| R2-07 | P1 | 实例卡片 7 个操作按钮无 accessible name | a11y 树仅为 `button`；依赖 tooltip | **T1 扩展** |
| R2-08 | P1 | 属性标签 70px ellipsis | 实机截断三处 | **T4** |
| R2-09 | P1 | 登录表单仅 placeholder 作标签 | DevTools：`labels: []`, `ariaLabel: null` | 跨项目 LoginView；备注 |
| R2-10 | P1 | 部分 Widget 名仍英文混排 | Carousel / Countdown / Parallel / Approval Process | **T0 附带** 或独立小修 |
| R2-11 | P2 | locale 重复 key 警告 | Vite：`widgetProps` duplicate in zh/en | **T0 附带** |
| R2-12 | P2 | 无缩略图卡片空白占位弱 | Instances「free」卡 | **T5** 空状态体系可覆盖 |
| R2-13 | P2 | 卡片操作图标过密（7 个） | 认知负担；无文字 | **T8/后续** 溢出菜单可选 |

### R2 做得好的地方（实机）

- 登录成功后主内容区中文正常（「实例管理」「新建」「筛选 Tab」）
- Editor 三栏 IA 清晰；未选中 Widget 时右侧展示「画布配置」合理
- 大量 Toolbar 按钮已有中文 `aria-label`（优于 R1 悲观估计）
- 布局模式有 `role=status` + live region
- 活跃壳无明显 neon glow（企业蓝主路径基本正确）

---

### Task 0: 补齐缺失 i18n key（R2 P0） — ✅ R3 Pass

**Priority:** P0（**最先做**）  
**Estimate:** 2–3h  
**Status:** Done in `da73e1a` · R3 verified  
**Depends on:** —  
**Files:**
- Modify: `src/locales/editor-zh-CN.ts`, `src/locales/editor-en-US.ts`
- 对照引用：`src/components/AppLayout.vue`, `src/views/NotFoundView.vue`, `src/views/WidgetMarketView.vue`, `src/views/EditorViewToolbar.vue`, canvas/zoom 相关组件
- 顺手：去掉重复的 `widgetProps` 键（Vite 已告警）

**Produces:** 侧栏/404/市场/语言切换/画布 hint/zoom 不再显示 `editor.xxx` raw key

**必须补齐的 key 组（R2 实测出现）：**
- `editor.appLayout.{brand,instances,templates,widgetMarket,language,backHome,backHomeTitle}`
- `editor.errorPage.{notFoundMessage,backHome}`
- `editor.widgetMarket.{title,subtitle,searchPlaceholder,groupAll,...}`（按 WidgetMarketView 引用全量补）
- `editor.toolbar.{langEn,langZh,language}`
- `editor.canvas.{flowAppendHint,dragReorder}`
- `editor.zoomIndicator.{zoomOut,zoomIn}`

- [x] **Step 1:** `rg "t\\(['\\\"]editor\\.(appLayout|errorPage|widgetMarket|zoomIndicator)" src` 列出全部引用
- [x] **Step 2:** 在 zh-CN / en-US 对称补全；跑既有 `localeCoverage` 测试若存在则扩展断言
- [x] **Step 3:** 浏览器回归：`/login` 触发的 404（或直开 NotFound）、`/instances` 侧栏、`/widget-market` 页头、编辑器语言钮与画布 hint
- [ ] **Step 4:** Commit：`fix(i18n): add missing editor shell locale keys`

**Verify:**
```bash
# 页面上不应再出现 raw key
rg -n "appLayout:|errorPage:|widgetMarket:|zoomIndicator:|flowAppendHint" src/locales/editor-zh-CN.ts
pnpm test -- src/__tests__/localeCoverage.spec.ts
```

**Anti-patterns:** 不要改 `t()` 调用点去硬编码中文；不要只补 zh 不补 en。

---

### Task 1: 补齐残留 icon 无障碍标签（R2 修订）

**Priority:** P0  
**Estimate:** 1–2h  
**Depends on:** T0（语言按钮文案先可用）  
**Files:**
- Modify: `src/views/EditorViewToolbar.vue`（R2 实测仍有 **6** 个 unlabeled 28×28 按钮）
- Modify: `src/views/InstancesView.vue`（卡片操作按钮补 `aria-label`，tooltip 保留）

**Produces:** Toolbar 残留 + 实例卡片操作均可被读屏命名

- [x] **Step 1:** 对 Editor 页 `document.querySelectorAll('button')` 筛无 `aria-label/title/text` 的项，逐一补齐
- [x] **Step 2:** Instances 卡片 7 操作（编辑元数据/设计器入口/预览/历史/发布/导出/删除）全部 `:aria-label`
- [x] **Step 3:** a11y snapshot 中不再出现无名 `button`
- [ ] **Step 4:** Commit：`fix(a11y): label remaining toolbar and instance card icon buttons`

**Verify:** DevTools a11y 树中实例卡与 Toolbar 无空名 button

---

### Task 2: 全局 focus-visible 焦点环

**Priority:** P0  
**Estimate:** 1–2h  
**Depends on:** —（可与 T0/T1 并行）  
**Files:**
- Create: `src/styles/a11y.scss`（或写入现有全局入口）
- Modify: `src/main.ts`（import）
- Modify: `src/views/EditorView.module.scss`（nameInput `outline: none`）
- Modify: `src/components/Editor/PageTabBar.module.scss`
- Modify: `src/views/WidgetMarketView.vue`（scoped 样式）
- Skip / careful: `src/widgets/richtext/style.module.scss`

**Produces:** 键盘聚焦可见；鼠标点击不强制粗环（`:focus-visible`）

- [x] **Step 1:** 新增全局 `:focus-visible` 规则
- [x] **Step 2:** 现有 `outline: none` 改为保留 focus-visible
- [x] **Step 3:** Tab 遍历 Toolbar / 名称输入 / 属性面板
- [ ] **Step 4:** Commit：`fix(a11y): restore focus-visible rings in editor chrome`

---

### Task 3: 清理视觉残留与死 Toolbar

**Priority:** P0（R2：活跃壳 glow 已弱，仍需清死代码防回归）  
**Estimate:** 2–3h  
**Depends on:** T1 完成后更安全  
**Files:**
- Delete: `src/styles/tech-theme.scss`（确认零引用）
- Delete: `src/components/Editor/EditorToolbar.vue`, `EditorToolbar.module.scss`
- Modify: `style.module.scss` / `EditorLeftPanel.module.scss` / `EventLogPanel.module.scss` / `ZoomIndicator.module.scss` — 去掉 dark glass fallback 与 `#00e676`

**Produces:** 无死 Toolbar；glass fallback 浅色；无硬编码 neon

### Task 5: 统一 EmptyState（文案 + CTA）

**Priority:** P1  
**Estimate:** 2–3h  
**Depends on:** —  
**Files:** EmptyState 组件 + 各 empty 调用点；可顺带强化 Instances 无缩略图占位（R2-12）

- [x] **Step 1:** 创建 `src/components/common/EmptyState.vue` 统一组件（icon/title/description/slot）
- [x] **Step 2:** 迁移 InstancesView、CredentialListView、WidgetMarketView、SubmissionListView 使用 EmptyState
- [ ] **Step 3:** Commit：`feat(ui): add unified EmptyState component and migrate views`

- [x] **Step 1–6:** 同前（rg 零引用 → 删 → 替换 fallback → 目视 → commit）
- Commit：`refactor(ui): remove tech neon leftovers and dead EditorToolbar`

---

### Task 4: 属性面板标签可读性

**Priority:** P1（R2 实机确认）  
**Estimate:** 1–2h  
**Depends on:** T3  
**Files:**
- Modify: `PropertyField.module.scss`, `style.module.scss`, `RequestConfigDialog.module.scss`, `PropertyField.vue`

**Produces:** 标签宽度 ≥88px；「列最小宽度」等不再 ellipsis 到不可读；截断时有 title

- [x] 将 `.label { width: 70px }` → `88px`（或 96）并补 `title`
- [x] 回归 Editor 画布配置区：三处「…」消失或 hover 全文
- Commit：`fix(ui): widen property panel labels and add truncation titles`

---

### Task 5: 统一 EmptyState（文案 + CTA）

**Priority:** P1  
**Estimate:** 2–3h  
**Depends on:** —  
**Files:** EmptyState 组件 + 各 empty 调用点；可顺带强化 Instances 无缩略图占位（R2-12）

---

### Task 6: prefers-reduced-motion + 热区/字号微调

**Priority:** P1（R2 确认 28×28）  
**Estimate:** 1–2h  
**Depends on:** T2  
**Files:** `EditorView.module.scss`, `a11y.scss`

- [x] 热区目标 ≥32px；badge ≥11px；reduced-motion 停 `autoSavePulse`

---

### Task 7: z-index token + 确认框统一

**Priority:** P2  
**Estimate:** 2h  
**Files:** `variables.scss`, `useAdvancedTableEvents.ts`

- [x] **Step 1:** 在 `variables.scss` 添加 z-index 层级 token（$z-base/$z-dropdown/$z-sticky/$z-dialog/$z-toast/$z-top）
- [x] **Step 2:** 将 `useAdvancedTableEvents.ts` 中 `window.confirm` 替换为 `ElMessageBox.confirm`
- [ ] **Step 3:** Commit：`refactor(ui): add z-index tokens and unify confirm dialogs`

---

### Task 8: 桌面断点策略 + 组件添加键盘/双击路径

**Priority:** P2（R2：1143px 已挤，可提前）  
**Estimate:** 3–4h  
**Depends on:** T5 更顺  
**决策（R2 确认）：** Editor 官方舒适宽度 ≥1280；`<1280` 默认收起一侧或双侧面板

- [x] **Step 1:** `useBreakpoint` composable 添加 `isNarrow` 计算属性（NARROW_THRESHOLD=1280）
- [x] **Step 2:** EditorView 使用 `watch(isNarrow)` 自动收起侧面板
- [x] **Step 3:** ComponentPanel 添加 `@dblclick` 双击直接添加组件到画布
- [ ] **Step 4:** Commit：`feat(ui): add narrow breakpoint auto-collapse and component double-click add`

---

### Task 9: 收尾验证

**Priority:** —  
**Estimate:** 1h  
**Depends on:** 已完成子集

**R2 回归清单（在原清单上追加）：**
- [x] 侧栏不再出现 `editor.appLayout.*`
- [x] `/widget-market` 标题/搜索/筛选已翻译
- [x] 编辑器无 `langEn` / `flowAppendHint` raw key
- [x] 属性「列最小宽度」等可读
- [x] 实例卡片操作可读屏命名
- [x] 1143px 下面板策略符合 T8 决策（若已做）
- [x] `pnpm test` / `pnpm build:check`

---

## Execution Order（R2 修订）

```
Week 1:  T0 → T1 ∥ T2 → T3
Week 2:  T4 → T5（可穿插启动 T8 窄屏）
Week 3:  T6 → T7
Week 4:  T8 收尾 → T9
```

**实际落地:** 已在单提交 `da73e1a` 一次合入 T0–T9（2026-08-11）。

**PR 切片（修订）：**
| PR | Tasks | 验收焦点 | R3 |
|----|-------|----------|-----|
| **PR-A0** | T0 | 无 raw i18n key | ✅ |
| **PR-A** | T1 + T2 | a11y 标签 + 焦点环 | ✅ |
| **PR-B** | T3 | 死代码/glass 清掉 | ✅（源码） |
| **PR-C** | T4 + T5 + T6 | 标签/空状态/热区 | ✅ |
| **PR-D** | T7 + T8 + T9 | 体系化 + 回归 | ✅ / T8 有残余 |

---

## Round 3 浏览器回归（2026-08-11 · 修复后）

**方法:** Chrome DevTools MCP · 重新登录 `admin` · Instances → Editor(`Flex 交互实测`) → Widget Market  
**视口:** 1143×943（`< 1280`，触发窄屏）  
**对照提交:** `da73e1a`

### R2 → R3 验收矩阵

| Task | R2 问题 | R3 实机结果 | 判定 |
|------|---------|-------------|------|
| T0 | 侧栏/404/市场/langEn/hint raw key | 侧栏「表单编辑器/实例管理/模板库/部件市场」；404「页面不存在/返回首页」；市场「部件市场/共 111 个部件」；语言「English」；画布「追加到流程末尾」；`rawKeys=[]` | ✅ Pass |
| T1 | Toolbar 6 无名 + 卡片无名 | Toolbar：关闭自动保存/撤销/重做/复制/删除/AI 皆有 label；卡片：编辑表单/设计器/预览/历史/发布/导出/删除 | ✅ Pass |
| T2 | focus-visible | `a11y.scss` 已全局引入 | ✅ Pass（代码+存在性） |
| T3 | tech-theme / 死 Toolbar / glass | 文件已删；活跃壳无 neon | ✅ Pass |
| T4 | 标签 70px 截断 | 实测 label **88px**；「列最小宽度/列最大宽度/内容最大宽度」全文可见 | ✅ Pass |
| T5 | EmptyState | 组件已落地，Instances/Market/Credential/Submission 已接入 | ✅ Pass（代码） |
| T6 | 热区 28px | Toolbar iconBtn 实测 **32×32** | ✅ Pass |
| T7 | z-index / confirm | `$z-sticky` 等 token + ElMessageBox 替换 | ✅ Pass（代码） |
| T8 | 窄屏双栏挤压 | `_leftClosed` / `_rightClosed`，宽 **0**；center 占满 1143 | ✅ 视觉 Pass；⚠️ 见残余 |
| T9 | 回归 | 本轮走查完成 | ✅ |

### R3 残余 backlog — 收口处理

| ID | 严重度 | 现象 | 收口结果 |
|----|--------|------|----------|
| R3-01 | P1 | 未登录跳 `/schema-platform/login` | ⏸ **延期·跨项目**（platform-shared） |
| R3-02 | P1 | 登录 placeholder-only | ⏸ **延期·跨项目**（LoginView） |
| R3-03 | P2 | 收起面板仍进 a11y 树 | ✅ `aria-hidden` + `inert`（`464e466`） |
| R3-04 | P2 | isNarrow 无 resize | ✅ matchMedia 订阅（`464e466`） |
| R3-05 | P2 | Carousel 等英文名 | ✅ 轮播图/倒计时等（`464e466`） |
| R3-06 | P3 | Toolbar ± 仅符号 | ✅ 「缩小/放大」aria（`464e466`） |
| R3-07 | P3 | 卡片 7 图标过密 | ⏸ **延期·体验增强** |
| R3-08 | P3 | 双缩放控件 | ⏸ **延期·体验增强** |

### 收口后延期（不阻塞本轮）

| 项 | 归属 | 说明 |
|----|------|------|
| authPaths 本地登录 URL | platform-shared | standalone → `/login` |
| LoginView 可见 label | platform-shared | 非 placeholder-only |
| z-index 硬编码全局替换 | editor 后续 | token 已定义，替换另开任务 |
| 实例卡溢出菜单 / 单一缩放入口 | editor 后续 | P3 体验 |

---

## Path to A（已迁出 · 勿在此更新）

活跃任务 A1–A12 与 DoD 见：**[../2026-08-12-ui-ux-path-to-a.md](../2026-08-12-ui-ux-path-to-a.md)**  
看板：[editor-ui-ux-path-to-a.canvas.tsx](/Users/yangdongnan/.cursor/projects/Users-yangdongnan-work-schema-platform-server/canvases/editor-ui-ux-path-to-a.canvas.tsx)

### R3 分数（相对 R2）

| 维度 | R2 | R3 | 说明 |
|------|----|----|------|
| 整体 UX | B- | **B+** | 壳层可读性与 a11y 明显提升 |
| 可访问性 | C+ | **B** | 标签/焦点/热区到位；收起面板 a11y 与登录仍欠 |
| 视觉一致性 | B | **B+** | neon/死代码清除 |
| 信息架构 | A- | **A-** | 不变，窄屏策略已补 |

---

## Out of Scope / 跨项目备注

| 项 | 原因 |
|----|------|
| 完整 Dark Mode / 复活 tech-theme | 产品未确认 |
| 改 `platform-shared` `authPaths` 本地登录跳转 | **跨项目**；需切换到 shared 仓：standalone localhost 应回 `/login` 而非 `/schema-platform/login` |
| LoginView 可见 label | 组件在 platform-shared |
| 改 server / 独立 docs 仓 | 隔离规则 |
| PropertyPanel 信息架构大翻修 | 超出本轮 |

## 决策记录

| 决策 | 选择 | 原因 |
|------|------|------|
| 视觉真源 | 企业蓝扁平 | R2 确认活跃壳已走此路径 |
| tech-theme | 删除 | 未 import；防 copy-paste 污染 |
| EditorToolbar | 删除 | 零引用 |
| 触控 44px | 桌面折中 32–36 | R2 实测 toolbar 高 28–32 |
| 官方宽度 | ≥1280 | R2 在 1143 已挤 |
| i18n | T0 最高优先 | R2 用户可见故障 > 静态 a11y 猜测 |
| 文档落点 | `editor/plans/` | 独立 docs 仓不在本上下文修改 |
